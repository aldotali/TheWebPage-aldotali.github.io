/*
 * Progam name : Lab06d
 * Description : returns the binary representation of a decimal
 * @author : Aldo Tali 2150009
 * Version: 1.0;
 * 
 * */
import java.util.*;

public class Lab06d
{
    int generator = 0;
    String s = "";
    public boolean checkDigit( int num, int n)
    {
//        if ( n <= 1)
//            return true;
//        else if ( num / Math.pow( 10, n-2) == 0)
//             return ( (num/Math.pow(10,1)) < num% Math.pow(10,1));
//        else if ( num / Math.pow( 10, n-1) % 2 > 0)
//            return checkDigit( (int)(num % Math.pow( 10, n-1)), n-1);
//        else if ( (num / (Math.pow( 10, n-1)))% (Math.pow( 10, n-1)) < (num / Math.pow( 10, n-2)) % Math.pow( 10, n-2))
//            return checkDigit( (int)(num % Math.pow( 10, n-1)), n-1);
//        else 
//            return false;
        if ( n <= 1)
            return true;
        else if ( n <=2 )
            return ( num/10 < num%10);
        else if ((int)(num/(Math.pow(10,n-1))) < ((int)(num/Math.pow(10,n-2)))%10)
            return checkDigit((int)(num%(Math.pow(10,n-1))), n-1);
        //else if ( (num/(Math.pow(10,n-1)) < (num/Math.pow(10,n-2))%10))
            //return checkDigit(num%100, n-1);
        //else if ( (num/Math.pow(10, n-1)) < (num/(Math.pow(10, n-2)%10)))
           // return (checkDigit ((int)(num%Math.pow(10, n-1)), n-1));
        else return false;
    }
    
    public String evenNo( int n)
    {
        generator += 2;
        if ( generator < Math.pow(10,n-1))
        { generator = (int)(Math.pow(10,n-1));}
        
        if ( generator >= Math.pow(10,n))
        {
            generator = 0;
            return s;
        }
        
       // else 
//        {
//            if (generator >= Math.pow(10,n-2))
//        {
           else if (checkDigit( generator, n))
            return s + generator + " " + evenNo(n) +" ";
        else
            return s + ""+ evenNo(n) +" ";
//        }
//            return s;
//        }
       // return s;
    }  
}